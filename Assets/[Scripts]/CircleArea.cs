using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
public class CircleArea : MonoBehaviour
{
    void Start()
    {
      //  transform.DOScale(transform.localScale + Vector3.one / 2, 0.5f).SetLoops(-1, LoopType.Yoyo);
    }
}
